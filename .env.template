# GestionELS environment template
# --------------------------------
# Copy this file to `.env` and fill each placeholder with the value for your
# workspace. Local tooling (npm scripts, clasp, firebase) will read from
# these variables but secrets must continue to live in Script Properties.

# Apps Script deployment identifiers (align with appsscript.json)
CLASP_SCRIPT_ID="1xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"  # Mirrors appsscript.json > scriptId
CLASP_DEPLOYMENT_ID="AKfycbxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"      # Optional: current web app deployment for CI jobs
CLASP_ROOT_DIR="./apps-script"                                            # Match the folder pushed via `npm run clasp:push`

# Primary Google Apps Script web app endpoints used by the PWA and integrations
WEB_APP_URL="https://script.google.com/macros/s/AKfycb-your-web-app-id/exec"  # Maps to Configuration.gs > CFG_WEB_APP_URL / README.md setup
ADMIN_WEB_APP_URL="https://script.google.com/macros/s/AKfycb-admin-app-id/exec" # Optional: secondary deployments for staging

# Firebase Web configuration (mirrors firebase-config.template.js)
FIREBASE_API_KEY="AIzaSyYourKey"                 # Used by firebase-config.js: firebaseConfig.apiKey
FIREBASE_PROJECT_ID="els-services-littoral"      # Used by Configuration.gs > CFG_FIREBASE_PROJECT_ID
FIREBASE_APP_ID="1:1234567890:web:abcdef123456"  # Matches Firebase console Web App settings
FIREBASE_SENDER_ID="1234567890"                  # Messaging sender ID for FCM web push
FIREBASE_MEASUREMENT_ID="G-XXXXXXXXXX"          # Optional analytics identifier

# Google resources consumed by Configuration.gs via Script Properties
ID_FEUILLE_CALCUL="1aYourSheetId"               # Script Property ID_FEUILLE_CALCUL (main Google Sheet)
ID_CALENDRIER="your-calendar-id@group.calendar.google.com" # Script Property ID_CALENDRIER
ID_DOCUMENT_CGV="1aYourCgvDocId"                # Script Property ID_DOCUMENT_CGV (Conditions Générales de Vente)
ID_MODELE_FACTURE="1aYourInvoiceDocId"          # Script Property ID_MODELE_FACTURE (Google Docs template)
ID_DOSSIER_ARCHIVES="1aYourArchiveFolderId"     # Script Property ID_DOSSIER_ARCHIVES (Drive archive)
ID_DOSSIER_TEMPORAIRE="1aYourTempFolderId"      # Script Property ID_DOSSIER_TEMPORAIRE (temporary uploads)
ID_DOSSIER_FACTURES="1aYourInvoicesFolderId"    # Optional override for FACTURES_FOLDER_ID in Configuration.gs
ID_FACTURES_DRIVE="1aYourInvoiceExportsId"      # Required by Setup_Checks.gs REQUIRED_PROPS

# Business identity (stored in Script Properties via the admin setup flow)
NOM_ENTREPRISE="EL Services Littoral"           # Display name used by Configuration.gs > NOM_ENTREPRISE
ADRESSE_ENTREPRISE="1 Rue Exemple, 83000 Toulon" # Postal address for invoices
EMAIL_ENTREPRISE="contact@example.com"          # Customer facing email (Configuration.gs > EMAIL_ENTREPRISE)
ADMIN_EMAIL="admin@example.com"                 # Administrative notification email (Configuration.gs > ADMIN_EMAIL)
SIRET="00000000000000"                           # Legal identifier stored in Script Properties (Setup_Checks.gs)
ELS_SHARED_SECRET="set-a-long-random-secret"    # Shared secret for webhook/auth flows referenced in Setup_Checks.gs

# Optional Drive branding assets (match Configuration.gs > DRIVE_ASSET_IDS)
ID_LOGO_FACTURE="1aYourInvoiceLogoId"           # Overrides FACTURE_LOGO_FILE_ID if defined
DRIVE_ASSET_CAPSULE_1X="1aCapsule1x"           # Optional: populates DRIVE_ASSET_IDS.CAPSULE_1X when DRIVE_ASSETS_ENABLED
DRIVE_ASSET_CAPSULE_2X="1aCapsule2x"
DRIVE_ASSET_BLISTER_1X="1aBlister1x"
DRIVE_ASSET_BLISTER_2X="1aBlister2x"
DRIVE_ASSET_ALUMINIUM_1X="1aAluminium1x"
DRIVE_ASSET_ALUMINIUM_2X="1aAluminium2x"

# Local tooling helpers
FCM_SERVICE_ACCOUNT_JSON="./secrets/fcm-service-account.json"  # Path consumed by scripts generating FCM JWTs
GOOGLE_APPLICATION_CREDENTIALS="./secrets/google-service-account.json" # For local utilities interacting with Drive/Sheets
